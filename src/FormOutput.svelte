<script>
    import {user} from "./stores";

</script>

<p>Name: {$user.name}</p>
<p>Email: {$user.email}</p>
<p>Phone Number: {$user.phone}</p>
<p>*NOTE: Stores are not limited to just the template. For example, the User Name is being capitalized via JavaScript, but is using the <code>store</code> variable.</p>
